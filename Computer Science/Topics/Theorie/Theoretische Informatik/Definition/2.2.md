Let $\sum$ be an [[2.1|Alphabet]], $n \geq 0$ and $a_1, a_2, \text{ ... }, a_n \in \sum$:  Length of a word $w$ := $a_0a_1 \cdot \cdot \cdot a_m$ = |w| = n

Let $\sum$ be an [[2.1|Alphabet]], $n \geq 0$ and $a_1, a_2, \text{ ... }, a_n \in \sum$ :  $\sum^n := {w | w \text{ if a word over } \sum  \text{ with } |w| = n} \text{. It holds }\sum^0=\epsilon$

Let $\sum$ be an [[2.1|Alphabet]], $n \geq 0$ and $a_1, a_2, \text{ ... }, a_n \in \sum$ :  concatenation of words $w = b_0b_1\cdot \cdot \cdot b_m$ and $v = v_0v_1 \cdot \cdot \cdot v_n$ over the alphabet \sum := $v \cdot w =\text{(df) } b_0b_1 \cdot \cdot \cdot b_mv_0v_1 \cdot \cdot \cdot v_n$

Let $\sum$ be an [[2.1|Alphabet]], $n \geq 0$ and $a_1, a_2, \text{ ... }, a_n \in \sum$ :  formal language over $\sum$ := subset of $\sum^*$






